= content_for :left_title_buttons do
  = link_to :back do
    paper-icon-button icon="arrow-back"

.lemon-post-form
  h1 New Post

  = render 'form', post: @post

javascript:
  if (!/new/.test(window.location.href)) {
    window.history.replaceState('data', '', window.location.href + '/new');
  }
